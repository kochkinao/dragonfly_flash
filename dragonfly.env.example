# Copy to dragonfly.env and fill real values.
DRAGONFLY_ACCESS_TOKEN=
# Required by Dragonfly comment endpoint /api/get_comments/<post_id>?user_id=<id>.
# Account-pool JWTs can also expose this as `sub`, but explicit is safer.
DRAGONFLY_USER_ID=
# Preferred long-term auth: export browser/session cookies in Mozilla/Netscape format.
# If this file is set, cookie auth is used instead of legacy JWT.
DRAGONFLY_COOKIE_FILE=/path/to/dragonfly_cookies.txt
# Optional: JSON account pool for failover on Dragonfly 401/auth failures.
# When set, this takes priority over DRAGONFLY_COOKIE_FILE for API calls.
# Copy dragonfly_accounts.example.json to a secure server-local path and chmod 600.
DRAGONFLY_ACCOUNTS_FILE=/path/to/dragonfly_accounts.json
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=@dragonfly_flash
TELEGRAM_ALERT_CHAT_ID=
# Optional admin control via the same Telegram bot. Only this Telegram user_id can run /set, /status, /get.
TELEGRAM_ADMIN_USER_ID=
# Optional: linked channel discussion group id for future comment mirroring.
TELEGRAM_DISCUSSION_CHAT_ID=
# Optional: target channel for posts that reach the likes threshold.
TELEGRAM_BEST_CHAT_ID=

